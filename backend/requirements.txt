# FastAPI and ASGI server
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
pydantic-settings==2.2.1

# Database
sqlalchemy==2.0.30
psycopg2-binary==2.9.9
alembic==1.13.1

# Supabase (PostgreSQL on GCP)
supabase==2.10.0

# Google Gemini AI (New SDK for gemini_analyzer.py)
google-genai

# Google Gemini AI (Old SDK - still used by other services)
google-generativeai==0.8.3

# HTTP clients
httpx==0.27.0
requests==2.31.0

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9

# Environment & Config
python-dotenv==1.0.1

# Email Service
sendgrid==6.12.5

# Payment Processing
stripe==11.1.0

# Logging & Monitoring
loguru==0.7.2
sentry-sdk==2.1.1

# Data validation & processing
email-validator==2.1.1
phonenumbers==8.13.35
numpy==1.26.4
json-repair==0.52.4

# Testing
pytest==8.2.0
pytest-asyncio==0.23.6
pytest-cov==5.0.0
pytest-mock==3.14.0
httpx==0.27.0

# Code quality
black==24.4.2
pylint==3.1.0
flake8==7.0.0
mypy==1.10.0

# Utilities
tenacity==8.2.3  # Retry logic
cachetools==5.3.3  # Caching
python-dateutil==2.9.0

# CORS
fastapi-cors==0.0.6

# Rate limiting
slowapi==0.1.9

# Premium Features Dependencies
# Firebase Admin SDK (Authentication)
firebase-admin==6.5.0

# File Parsing
PyPDF2==3.0.1
python-docx==1.1.0

# Redis (Caching & Rate Limiting)
redis==5.0.1
hiredis==2.3.2  # C parser for Redis (faster)

# Neo4j (Graph Database for Talent Graph)
neo4j==5.14.1

# Background task scheduling
APScheduler==3.10.4

# Two-Factor Authentication
pyotp==2.9.0  # TOTP implementation
qrcode[pil]==7.4.2  # QR code generation
